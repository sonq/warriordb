<%# app/views/sessions/new.html.erb %>
<div class="mx-auto md:w-2/3 w-full max-w-md p-6">
  <h1 class="font-bold text-4xl mb-8 text-center">Oturum aç</h1>

  <%= form_with(url: sign_in_path, class: "space-y-6") do |form| %>
    <% if flash[:alert] %>
      <div class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <div>
      <%= form.label :email, class: "block text-sm font-medium text-gray-700" %>
      <%= form.email_field :email, 
          class: "mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500" %>
    </div>

    <div>
      <%= form.label :password, class: "block text-sm font-medium text-gray-700" %>
      <%= form.password_field :password, 
          class: "mt-1 block w-full rounded-md border border-gray-300 px-3 py-2 shadow-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500" %>
    </div>

    <div>
      <%= form.submit "Giriş yap", 
          class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
    </div>
  <% end %>

  <div class="mt-6 text-center text-sm text-gray-500">
    Üyeliğin yok mu? 
    <%= link_to "E üye ol.", sign_up_path, class: "font-medium text-blue-600 hover:text-blue-500" %>
  </div>
</div>